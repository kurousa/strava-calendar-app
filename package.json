{
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "@google/clasp": "^3.3.0",
    "@types/google-apps-script": "^2.0.8",
    "@types/node": "^25.6.0",
    "@vitest/coverage-v8": "^4.1.4",
    "shx": "^0.4.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "clean": "shx rm -rf dist",
    "build": "pnpm run clean && tsc && shx cp appsscript.json index.html dist/",
    "push:dev": "pnpm run build && clasp push --project .clasp-dev.json",
    "push:prod": "pnpm run build && clasp push --project .clasp-prod.json",
    "open:dev": "clasp open --project .clasp-dev.json",
    "open:prod": "clasp open --project .clasp-prod.json",
    "pull:dev": "clasp pull --project .clasp-dev.json",
    "pull:prod": "clasp pull --project .clasp-prod.json"
  }
}
