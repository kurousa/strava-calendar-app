🎯 **What:** The main entry points `main()` and `processActivityToCalendar()` were lacking tests.
📊 **Coverage:** Added tests covering early exits when no activities or calendar are found, handling duplicates, and creating events with/without distances.
✨ **Result:** Increased coverage of main workflow and isolated logic in vitest mock context.
